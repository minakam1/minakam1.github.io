# Repository guide

Read `docs/agent-memory/INDEX.md` before starting non-trivial work, then open only the relevant memory file.

The GitHub Pages artifact is assembled by `.github/workflows/pages.yml`. Keep unpublished source projects out of `_site`, preserve unrelated working-tree changes, and update `docs/agent-memory/` when project state or deployment decisions change.
