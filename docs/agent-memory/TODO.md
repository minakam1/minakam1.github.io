# TODO

- Verify the next GitHub Pages workflow completes successfully.
- Verify `/` and `/gallery/` return 200 while `/blog/` remains unavailable.
- If the blog is reopened later, add it to the artifact explicitly and restore its homepage entry only then.
